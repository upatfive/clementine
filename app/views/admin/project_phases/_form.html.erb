<%= form_for(project_phase) do |f| %>
  <% if project_phase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_phase.errors.count, "error") %> prohibited this project_phase from being saved:</h2>

      <ul>
      <% project_phase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :project_id %>
    <%= f.text_field :project_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
