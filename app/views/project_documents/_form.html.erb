<%= form_for(project_document , html: { multipart: true }) do |f| %>
  <% if project_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_document.errors.count, "error") %> prohibited this project_document from being saved:</h2>

      <ul>
      <% project_document.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :project_id %>
    <%= f.text_field :project_id %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.file_field :doc %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
